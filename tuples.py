# a tuple is similar to a list but it is immutable(it cannot be manipulated)
# in tuples use parenthesis
days_of_the_week = ("Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday")
print(days_of_the_week[1])
print(days_of_the_week.count("Tuesday"))
